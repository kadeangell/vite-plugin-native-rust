{
  "name": "example-qwik",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite --mode ssr",
    "build": "npm run build.client && npm run build.server",
    "build.client": "vite build",
    "build.server": "vite build -c adapters/vercel-node/vite.config.ts",
    "preview": "node server/entry.node-server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@builder.io/qwik": "^1.20.0",
    "@builder.io/qwik-city": "^1.20.0",
    "vite-plugin-native-rust": "*"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.7.2",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.5"
  }
}
