{
  "name": "vite-plugin-native-rust-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Monorepo for vite-plugin-native-rust — import Rust directly in Vite SSR server code.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kadeangell/vite-plugin-native-rust.git"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w vite-plugin-native-rust && npm run build -w example-react-router",
    "build:plugin": "npm run build -w vite-plugin-native-rust",
    "test": "npm test -w vite-plugin-native-rust",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
