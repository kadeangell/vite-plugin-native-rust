{
  "name": "fixture-vitest-consumer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Integration fixture: vitest 4 (rolldown-vite) on Vite 8 importing a Rust crate through rustPlugin() and rustTestStub. Installed in isolation so it can run Vite 8 without conflicting with the vite-6 example.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.7.2",
    "jsdom": "^25.0.1",
    "vite": "^8.1.3",
    "vite-plugin-native-rust": "file:../../..",
    "vitest": "^4.1.9"
  }
}
